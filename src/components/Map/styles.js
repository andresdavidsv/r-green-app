import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  centerButton: {
    position: 'absolute',
    bottom: 20,
    right: 20,
  },
  brushButton: {
    position: 'absolute',
    bottom: 80,
    right: 20,
  },
});

export default styles;
